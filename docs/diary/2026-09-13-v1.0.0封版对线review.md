# 2026-09-13 v1.0.0 封版对线 review

## 起因

- ohmycloud WSL 总台原子部署验收（C 节要求三仓 git 净）进入封版交付段，用户按 AGENTS 写 Rust 对线规则发起 herdr 右侧 codex review：v1.0.0..HEAD 全量过审、结论一致才准封版。
- 范围核对：tag v1.0.0（ca66a40）后共三笔：ef44915（src/selfupdate.rs，replace_exe 首次落位直接写新件，唯一实质代码改动）、54c3188（单 token 改名 `ome-new` 换 `ark-new`）、508ae70（GOAL/TODO 状态行）。工作树净、与 origin/main 同步。

## 逐条结论

- codex 审 ef44915 首落分支：语义一致，通过。首落条件 `!exe.exists()` 时部署位无运行中锁定对象；运行中副本走 `replace_deployed_and_current` 的 `current.exists()` 守卫仍进原改名链，未被新分支吞掉。分支内 `create_dir_all` 加 `copy` 与 init 侧既有 `selfdeploy::deploy_copy` 同构，未自创新口径。
- 旧件清理：nit。上轮中断留下孤立 `ark.exe.old` 而 `ark.exe` 缺失时本分支不碰它，残留非自愈（要等下一次成功替换起手清），影响仅磁盘垃圾。
- 失败回滚：nit。首落 copy 失败无恢复点，会留半截 `ark.exe` 在 PATH 可执行位上，比缺位更脏；原分支有 `rename(old, exe)` 回滚。建议首落 copy 出错时删残留，或改临时件加 rename。
- 原子性：nit。首落直接 copy 到最终路径非原子；此刻目标不存在，本可用同目录临时件加 rename 拿原子落位。沿用既有 Windows 非原子语义，读到截断 exe 的窗口低概率，非阻断。
- 冗余建目录：nit。调用方已 `create_dir_all(deploy.parent())`，分支内再建一次，幂等无害。
- TOCTOU：通过。`exists()` 非原子探测但单进程无并发写者；误报 false 时随后 copy 同样失败，不会假成功。
- Unix 侧：通过。copy、chmod 755、rename 原子链原样保留。
- 54c3188：通过，单 token 语义不变；随记 nit：老版本遗留的 `ark.ome-new` 临时件不会被清，无害。
- 508ae70：通过，仅状态行，叙述与 ef44915 实修对得上。
- 范围外备注（非本次改动引入，不计入结论）：git 通道下直接从 `target\release\ark.exe` 运行时 `replace_exe(current=bin, bin)` 先改名再 copy 自身（新件已不在）而失败、被调用方 `let _` 吞掉后回滚，无净损，属既有行为。

## 验证

- [实证] codex 本机实跑：`cargo test --lib selfupdate::` 6 passed（含 `替换exe_首次落位_无前代直接写`）；`cargo clippy --lib --tests` 干净。
- [实证] codex 本机实跑门禁：rumdl check GOAL.md TODO.md 无问题；mdcharlint 通过；md-ref-scan 63 文件 0 断链；md-heading-scan 0 括号标题。
- 测试落位合规：replace_exe 为私有 fn，按 R004 同文件 `#[cfg(test)] mod tests` 收单元属正确落点。

## 裁定

- 结论一致：无阻断项，codex 明确回复封版无阻断。v1.0.0 封版交付放行，回执提交号 508ae70。
- nit 四条（孤立 `.old` 非自愈、首落失败无恢复点、首落非原子、分支内重复建目录）留档后续裁，不阻本次交付。

## README 与仓库描述重写

- 用户令：README 精简、专注安装部署使用方法，删铺陈背景；GitHub 仓库描述一句话。落 `3b51b6a`：删更名背景段与命令速查大表，改按安装、install 链、catalog 四重门、供给清单、镜像源、输出契约六节组织。
- 「四重门」口径首次上 README：仓内成文一直是「三重门」（边车 sha 锚、schema 解析、minisign 验签，R015）加 D40 seq 单调门，README 层并称四重门，与 CHANGELOG D40 条（三重门后读 seq 拒收）口径一致。
- 仓库描述经 `gh repo edit` 改写并回读验证生效；来件写 `raystyle/ark_rs`，实际以 remote 实名 `raystyle/ark-rs` 执行。门禁四件套绿后提交。
