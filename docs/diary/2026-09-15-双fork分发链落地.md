# 2026-09-15 双 fork 分发链落地

## OfficeCLI fork v1.0.151 发布

- 侦察反转：150 win 启动崩初判「trim 裁 Pipelines」被 codex 实证推翻——149/150/HEAD 三方 bundle 闭包一致、显式引用被 SDK NU1510 摘除产物零字节差、上游 GitHub 与镜像资产逐字节同 sha；codex 以「物理移除 dll」复现一字一差之栈，真凶改判**截断文件类**（bundle 尾部截断可启动、首程序集加载即 FNF）。
- 落地（codex 两轮对线全修）：`__selftest__` 响亮冒烟（RunRefresh 全身吞异常、原 `__update-check__` 冒烟按构造不可能失败）；全 8 岗 trim 闭包静态断言；musl 非静态撤真跑（我臆断「静态件」之误）；installer 双脚本 `OFFICECLI_LOCAL_BINARY` 离线短路（镜像未播加 draft 不可见期不红）；InformationalVersion 血统标记；mac 签名 HAS_CERT 门（fork 无上游 Apple 证书）。
- 发版：dispatch 八岗绿后 tag，**fork 仓 tag push 不触发 Actions**（dispatch 正常，重推无效，actions permissions=all 无限制），走 `gh workflow run --ref <tag>` 等效通道（同 commit 同 ref 语义，release job 照走）九岗绿，draft publish，v1.0.151 九资产正式到货（digest 清单入 ohmycloud 回执）。
- 本机 `~/.local/bin/officecli` 顺 installer 冒烟升 1.0.151 fork（on-PATH 原地升级语义实证）。

## oxvg fork CI 链

- build.yml 蓝本 ark 语义：三岗（linux gnu、mingw 交叉 win-gnu、mac arm64）、oxvg-版本-triple 资产名对齐 catalog pin、sha256sum 边车、dev prerelease 加 v* tag 双通道、零 R2 secrets。
- codex 快核全绿修 G1（CRLF 检出版本带 `\r` 加 workspace 化空版本静默绿——sed 吃 CR 加空版本 exit 1）G2（三岗并发 release create 竞态——create 败后 view 重试幂等）。
- 用户裁「我们只要 cli」：上游六工作流全撤（wasm/jsx/napi/size/benchmark/checks——checks 含 setup-node 面），CI 面收敛 build 一条线；packages 与 node 工程文件保留保上游 merge 面。
- build 三岗绿，dev release 六资产到货（sha 清单入 ohmycloud 回执）；tag v0.0.7 待 ohmycloud catalog 滚版时点。

## 跨仓待办

- OfficeCLI G3 契约：镜像段形态（GitHub releases 形态即插即用，ark 惯例 version 段形态则改读序）、只播 fork 制品硬约束、catalog 切 fork 三平台件；lan-win 实跑 win-x64（`--version` 加 `__selftest__`）。
- oxvg：catalog 补 mac 字段（mac_extract=targz-bin）、re-seed 同批重算全 pin（CI 产物与手工构建同名同版不同 sha）。

## 教训

- 断言「静态二进制」前先 `file` 看一眼（musl 交叉产物带动态 loader）。
- fork 仓 tag push 可能不触发 Actions：发版走 `gh workflow run --ref <tag>` 等效通道，结论仍以 run conclusion 权威字段为准。
