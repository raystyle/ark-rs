# 2026-09-15 ADR0001 文档体系迁移批一

## 裁定与制度件

用户三问裁定（方案 A 全量迁移、D 存量冻结索引新立 ADR、mistakes 并入 ADR）。批一落地：

- `docs/adr/` 与 `docs/requirements/` 骨架（README 索引加模板，dev-evo 模板拷贝）。
- ADR-0001 文档体系全量迁移 dev-evo（Context/Decision/Consequences，六条决策与回滚点）。
- REQ-0001 迁移需求（Scenario/Criteria 八判据，draft 起）。
- AGENTS.md 四段重写为五节合同（Commands/Must/Must not/Read first/环境），全部现行规则语义无损重排：意图路由入 Commands、工作节奏与写作编码正面义务与义务表入 Must、禁止面入 Must not、资源索引入 Read first、平台矩阵与分支模型与门禁入环境；四原语核对语义改目标态（REQ 与 TODO，迁移期含 PRD 冻结索引）。
- PRD 头部冻结声明（D01 至 D49 只读索引，D50 起走 ADR）。

## 门禁收紧与两制调和

- dev-evo 禁字严于本仓四件套（Unicode 箭头与连接号入禁）：diary 历史存量八处清剿（三轮报三轮清，check 报告截断逐轮显形）。
- PE-12 只扫 AGENTS 与 docs 各 README：AGENTS 新文两处裸脚本名补路径；模板引用去反引号改描述文字。
- rumdl MD025 与 dev-evo 模板形态（frontmatter title 加正文 h1）冲突：`.rumdl.toml` 配 `front_matter_title = ""` 调和，ADR/REQ 模板照用。

## 验证

- check.py 基线 5 PASS 加 5 FAIL，批一后 **12 PASS 全合规零 SKIP**。
- 本仓四件套绿（rumdl 83 文件零问题、禁字零、断链零、括号标题零）。
- 后续批：批二需求面迁移（TODO 与 PLAN 转 REQ、GOAL 定位句并入 AGENTS 后退役）；批三 INDEX 拆解与 llms.txt；批四 M 系列并入 ADR 与全仓引用替换。
