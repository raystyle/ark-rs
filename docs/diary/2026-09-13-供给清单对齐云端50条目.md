# 2026-09-13 供给清单对齐云端 50 条目

## 起因

- 用户纠 README 供给清单行「操作编排（2）| ome（自管条目）、herdr」：已无 ome 主条目概念，新邻是 hst，且 hst 无编排功能；herdr 与 omc 已就 hst 同步达成一致。
- 核权威面：ohmycloud seed 与云端 env.ohmygh.com 均 50 条目在册（本机 catalog 副本 49 条滞后一轮，未含 hst）。seed 口径：`ark` 自管主条目（category base）、`ome` 更名过渡条目（存量端水位清零后退役）、`hst` 部署配置与诊断 CLI（原 oma 2026-09-12 更名，category cli）、`omc` 云与内网控制台 CLI（category derived）、`herdr` 会话宿主（base）。

## 修法

- README 供给清单：计数 47 改 50；原「操作编排（2）」一行拆四行：自管与过渡（2，ark 主加 ome 过渡）、操作编排（1，herdr）、agent 配置与诊断（1，hst）、云端控制台（1，omc）。hst 不入编排类，按 seed 描述归类。
- AGENTS 两处计数同步（定位节 50 工具新公式、管理对象节 50 工具名录）；R001 两处同步（术语节自管双条目表述、category 行 taxonomy 在册面：base 含 ark/ome、cli 含 hst、derived 在册 browser-harness 与 omc）。
- 历史日期行（CHANGELOG/GOAL/TODO/diary 2026-09-02 的 37/47 计数与 taxonomy 记录）按史实保留不动。

## 验证

- [实证] 云端 `https://env.ohmygh.com/ark/catalog/tools.toml` 实拉 50 条目，ome/ark/hst/omc 四节齐；seed 与云端一致（本机副本 49 属 TTL 滞后，非分歧）。
- 门禁四件套绿后提交。

## 纪律

- README 供给清单计数与归类是数据面镜像，云端 seed 动了 README 就欠账；本轮 ome 换 ark 双条目与 hst/omc 入册已欠两轮（47 时 omc 已在册未列）。后续 omc 侧清单演化时同步对表，或考虑把计数从活文档里淡化（写「名录权威在云端」而非硬数字）。
