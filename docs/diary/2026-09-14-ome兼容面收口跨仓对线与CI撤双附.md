# 2026-09-14 ome 兼容面收口跨仓对线与 CI 撤双附

## 经过

ohmycloud 三轮 herdr 对线驱动本轮收口：

1. **问询**：oma/stable 与 oma/dev 段删净后，问 ark 引擎面对 catalog `[tools.ome]` 节的代码级消费点、存量端口径、下架安全性。
2. ** ark 答复**（三问）：零硬编码消费（self update 走官方 API 加通道段边车，不读节；唯一节级消费 `is_ome_self` 按 extract 字段判定非按 id）；本仓可实证两端；建议直接下架。
3. **舰队对账回执**：omc 侧全舰队口径，装态加调用态双零（残尾 lan-mac 0.4.0 与 lan-linux2 0.3.0 已升 ark 1.2.1 清 ome）；唯一残留 lan-win 的 `ome.exe` 别名副本（D41 C 幂等重建产物），留本仓 compat 收口批停建。
4. **派单落地**：本仓撤 CI ome-* compat 双附与 ome/ 段灌写、selfdeploy 停建别名并清理存量副本；ohmycloud 侧已删 `[tools.ome]` 节、`[tools.ark]` extract 切 ark-self，桶内 ome/ 段 18 对象待本回执后删。

## 关键证据

- v0.3.0 / v0.4.0 / v0.4.2 的 `is_ome_self` 均只认 `ome-self`（git show 三连实证）；v1.x 双接受 `ome-self|ark-self`。extract 切 ark-self 在残尾收编前对 0.x 端会把 `[tools.ark]` 当普通条目，调用态全零为护栏，残尾收编后风险归零，故裁定不回滚。
- env 桶 `ome/catalog` 段仍有引擎回落消费点：`cloud_catalog_keys()` 读序 ark 主先、ome 兼容回落（catalog.rs），主键在位即不触达；全舰队 1.2.1 后可同批删，omc 侧消费面归 ohmycloud 自查（D37 权威在彼）。
- 残尾两台升级走的 ark/stable 镜像段拉取（omc 回执），ome/ 段非唯一通道，删段窗口安全。

## 落地

- `80c3923` chore: CI 撤 ome 兼容双附与 ome 段停写（build.yml 撤 compat 列与 upload、seed.py 撤 `--ome-*` 参数族与 OME_ASSETS、UA 统一 ark-seed、.tools/README 同步）。
- `591229f` feat: selfdeploy 停建 ome 别名并在部署升级时清理存量副本（platform 加 `remove_ome_alias` 与幂等双态单测；selfdeploy 与 selfupdate 调用点停建改清理；INDEX 与 README 同步）。
- 文档批：AGENTS 定位句、TODO D41 行、ROADMAP 收口注记、CHANGELOG Unreleased。

## 对线与验证

- 跨仓对线：ohmycloud 会话（herdr w4:p1）三轮往返，本 diary 即对线结论记录；推送前待 ohmycloud 复核 diff。
- `cargo test` 全绿（137 单测加集成面，含新增清理幂等双态）；文档门禁四件套绿（mdcharlint、md-ref-scan 71 文件零断链、md-heading-scan、rumdl 72 文件）。
- 未推远端（等对线复核与用户令）。
