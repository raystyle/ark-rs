# 2026-09-15 ADR0001 文档体系迁移批三

## INDEX 拆解与 llms.txt

- 五目录 README 索引建成：references（R 表）、guide（G 表）、research（S 表）、proven（归档规则）、diary（32 行一览，含迁移期新笔）。
- llms.txt 仓根建成：agent 检索面（定位、读序、目录结构、33 行代码文件位置表）。
- INDEX.md 退役为指针页（迁向速查）；AGENTS 查文档命令与 Read first 切 llms.txt。
- 顺带：rumdl 接入 docs 目录后 md-ref-scan 与 rumdl 首次全覆盖新 README（修复四处断链与六处格式，rumdl fmt 自动收）。

## 转换坑（迁移不是搬运实证）

INDEX 表行批量迁移时反斜杠路径只换前缀留了尾巴（references\R001、catalog\tools.toml），PE-12 按正斜杠解析断链暴露——批量路径转换必须全文替换字符而非前缀。

## 验证

check.py 12 PASS 零 SKIP；四件套绿。余批四（M 系列并入 ADR 与全仓引用替换）。
